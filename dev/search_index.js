var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Index","page":"API Reference","text":"","title":"Index"},{"category":"section","location":"api/#Docstrings","page":"API Reference","text":"","title":"Docstrings"},{"category":"constant","location":"api/#SlackClaw.UPLOAD_INSTRUCTIONS","page":"API Reference","text":"Appended to the system prompt when the upload helper and a Slack thread are available, so Claude knows it can post files/images back to the thread.\n\n\n\n\n\n","title":"SlackClaw.UPLOAD_INSTRUCTIONS"},{"category":"type","location":"api/#SlackClaw.ClaudeResult","page":"API Reference","text":"Result from a Claude CLI invocation.\n\n\n\n\n\n","title":"SlackClaw.ClaudeResult"},{"category":"type","location":"api/#SlackClaw.MonitorState","page":"API Reference","text":"Runtime state of a monitor: cursors, tracked threads, scheduled tasks, and the active task list. Created by start_monitor; persisted to .slackclaw_state.json in repo_dir across restarts.\n\n\n\n\n\n","title":"SlackClaw.MonitorState"},{"category":"type","location":"api/#SlackClaw.ScheduledTask","page":"API Reference","text":"A scheduled future invocation.\n\n\n\n\n\n","title":"SlackClaw.ScheduledTask"},{"category":"type","location":"api/#SlackClaw.SlackClawConfig","page":"API Reference","text":"Configuration for SlackClaw monitor.\n\nAll fields have sensible defaults. slack_bot_token, slack_channel_id, and app_token default to reading SLACK_BOT_TOKEN / SLACK_CHANNEL_ID / SLACK_APP_TOKEN from the environment. app_token is required — SlackClaw delivers messages exclusively over Slack Socket Mode. Run SlackClaw.setup() to provision the app and tokens.\n\n\n\n\n\n","title":"SlackClaw.SlackClawConfig"},{"category":"type","location":"api/#SlackClaw.SlackMessage","page":"API Reference","text":"Slack message representation and filtering.\n\n\n\n\n\n","title":"SlackClaw.SlackMessage"},{"category":"type","location":"api/#SlackClaw.ThreadSession","page":"API Reference","text":"Track a conversation thread's Claude session.\n\n\n\n\n\n","title":"SlackClaw.ThreadSession"},{"category":"method","location":"api/#SlackClaw._run_claude_once-Tuple{Base.AbstractCmd, Real}","page":"API Reference","text":"_run_claude_once(cmd, timeout_s) -> NamedTuple\n\nRun cmd once, capturing stdout and stderr separately and enforcing a hard timeout (SIGTERM on overrun). Never throws on a non-zero exit; the caller inspects exitcode / stdout / stderr / timed_out.\n\n\n\n\n\n","title":"SlackClaw._run_claude_once"},{"category":"method","location":"api/#SlackClaw.api-Tuple{}","page":"API Reference","text":"SlackClaw.api() -> String\n\nReturn this package's api_overview.md — an LLM-parseable API reference. Unexported; call as SlackClaw.api(). println(SlackClaw.api()) prints it.\n\n\n\n\n\n","title":"SlackClaw.api"},{"category":"method","location":"api/#SlackClaw.check_proactive!-Tuple{MonitorState}","page":"API Reference","text":"Check if a proactive run is due, and if so, run Claude with the proactive prompt.\n\n\n\n\n\n","title":"SlackClaw.check_proactive!"},{"category":"function","location":"api/#SlackClaw.chunk_text","page":"API Reference","text":"chunk_text(text, limit=SLACK_MAX_TEXT) -> Vector{String}\n\nSplit text into chunks of at most limit characters, preferring to break at a newline when one falls in the latter half of the window. Whitespace-only chunks are dropped.\n\n\n\n\n\n","title":"SlackClaw.chunk_text"},{"category":"method","location":"api/#SlackClaw.claim_listen!-Tuple{MonitorState, AbstractString, AbstractString}","page":"API Reference","text":"Claim a listen-channel message against that channel's cursor.\n\n\n\n\n\n","title":"SlackClaw.claim_listen!"},{"category":"method","location":"api/#SlackClaw.claim_primary!-Tuple{MonitorState, AbstractString}","page":"API Reference","text":"Claim a top-level message against the primary-channel cursor. Returns false if already seen.\n\n\n\n\n\n","title":"SlackClaw.claim_primary!"},{"category":"method","location":"api/#SlackClaw.claim_thread_reply!-Tuple{MonitorState, SlackClaw.ThreadSession, AbstractString}","page":"API Reference","text":"Claim a thread reply against the session's reply cursor.\n\n\n\n\n\n","title":"SlackClaw.claim_thread_reply!"},{"category":"method","location":"api/#SlackClaw.classify_socket_event-Tuple{Dict, SlackClawConfig, Any}","page":"API Reference","text":"classify_socket_event(event, config, tracked_threads) -> (route::Symbol, msg)\n\nPure routing decision for a Socket Mode message event. Routes:\n\n:primary       — top-level message in the monitored channel\n:thread_reply  — reply inside a tracked thread\n:listen        — top-level message in a configured listen channel\n:ignore        — everything else\n\nBot/self/empty filtering is NOT done here: the dispatcher applies should_process after claiming the cursor, mirroring the poll paths (cursors advance past bot messages too, which keeps reconciliation cheap).\n\n\n\n\n\n","title":"SlackClaw.classify_socket_event"},{"category":"method","location":"api/#SlackClaw.dispatch_listen_command!-Tuple{MonitorState, SlackMessage, String}","page":"API Reference","text":"Dispatch a message from a listen channel – only post to primary channel if Claude deems it relevant.\n\n\n\n\n\n","title":"SlackClaw.dispatch_listen_command!"},{"category":"method","location":"api/#SlackClaw.expire_threads!-Tuple{MonitorState}","page":"API Reference","text":"Drop threads idle beyond max_thread_idle_s, then enforce the max_active_threads count cap (oldest first). Every tracked thread costs a conversations.replies call per poll/reconcile cycle, so unbounded idle threads become permanent rate-limit pressure.\n\n\n\n\n\n","title":"SlackClaw.expire_threads!"},{"category":"method","location":"api/#SlackClaw.generate_manifest-Tuple{}","page":"API Reference","text":"generate_manifest(; app_name=\"SlackClaw\", description=...) -> String\n\nBuild a Slack app manifest (YAML) that pre-configures everything SlackClaw needs in one shot: bot scopes, message event subscriptions, and Socket Mode enabled. Paste it into https://api.slack.com/apps → Create New App → From an app manifest, so the human never has to toggle scopes or events by hand.\n\n\n\n\n\n","title":"SlackClaw.generate_manifest"},{"category":"method","location":"api/#SlackClaw.handle_socket_frame!-Tuple{Any, Any, Channel}","page":"API Reference","text":"Handle one websocket frame: parse, ack, and enqueue message events onto events for the dispatch consumer. Does no Slack API work itself — the read loop must never block on dispatch, or queued frames miss the ~3s ack deadline and get redelivered. Returns :ok to keep reading or :reconnect when Slack asks for the connection to be refreshed.\n\n\n\n\n\n","title":"SlackClaw.handle_socket_frame!"},{"category":"method","location":"api/#SlackClaw.idle_expired_threads-Tuple{Dict{String, SlackClaw.ThreadSession}, Int64, Float64}","page":"API Reference","text":"Sessions idle longer than idle_cutoff_s as of now (none when cutoff is 0).\n\n\n\n\n\n","title":"SlackClaw.idle_expired_threads"},{"category":"method","location":"api/#SlackClaw.load_state-Tuple{SlackClawConfig}","page":"API Reference","text":"Load persisted state from disk. Returns (threads, lastts, scheduled, listenlastts, lastproactive).\n\n\n\n\n\n","title":"SlackClaw.load_state"},{"category":"method","location":"api/#SlackClaw.parse_claude_output-Tuple{String, Int64}","page":"API Reference","text":"parse_claude_output(output::String, duration_ms::Int) -> ClaudeResult\n\nParse JSON output from claude --print --output-format json. The CLI returns a JSON array of event objects; the result is in the last entry with \"type\" => \"result\".\n\n\n\n\n\n","title":"SlackClaw.parse_claude_output"},{"category":"method","location":"api/#SlackClaw.parse_directives-Tuple{String}","page":"API Reference","text":"parse_directives(text::String) -> (Directive, String)\n\nExtract directive from response text. Returns the directive and cleaned text.\n\n\n\n\n\n","title":"SlackClaw.parse_directives"},{"category":"method","location":"api/#SlackClaw.parse_duration-Tuple{String}","page":"API Reference","text":"parse_duration(s::String) -> Int\n\nParse duration string like \"1h\", \"30m\", \"1h30m\" into seconds.\n\n\n\n\n\n","title":"SlackClaw.parse_duration"},{"category":"method","location":"api/#SlackClaw.parse_slack_messages-Tuple{Vector}","page":"API Reference","text":"parse_slack_messages(messages::Vector) -> Vector{SlackMessage}\n\nParse raw Slack API message dicts into SlackMessage structs.\n\n\n\n\n\n","title":"SlackClaw.parse_slack_messages"},{"category":"method","location":"api/#SlackClaw.poll_listen_channels!-Tuple{MonitorState}","page":"API Reference","text":"Poll listen-only channels for new messages, dispatching responses to the primary channel.\n\n\n\n\n\n","title":"SlackClaw.poll_listen_channels!"},{"category":"method","location":"api/#SlackClaw.post_response-Tuple{SlackClawConfig, AbstractString, AbstractString}","page":"API Reference","text":"Post a response to Slack, splitting long text across multiple thread messages.\n\n\n\n\n\n","title":"SlackClaw.post_response"},{"category":"function","location":"api/#SlackClaw.print_setup_instructions","page":"API Reference","text":"print_setup_instructions([io]; app_name=\"SlackClaw\", manifest_path=\"\")\n\nPrint the manifest and the grouped human-only browser steps. Every step here is done in one sitting at https://api.slack.com/apps — there is no back-and-forth.\n\n\n\n\n\n","title":"SlackClaw.print_setup_instructions"},{"category":"method","location":"api/#SlackClaw.reconcile_messages!-Tuple{MonitorState}","page":"API Reference","text":"Gap-fill reconciliation: fetch and dispatch anything newer than the cursors — primary-channel history, tracked thread replies, and listen channels. Socket Mode delivery is at-least-once with gaps across reconnects, so this runs on every (re)connect and every reconcile_interval_s; the claim_*! cursor gates drop anything the live socket already dispatched.\n\n\n\n\n\n","title":"SlackClaw.reconcile_messages!"},{"category":"method","location":"api/#SlackClaw.resolve_channel-Tuple{SlackClawConfig, AbstractString}","page":"API Reference","text":"Resolve a channel name (dev, #dev) or ID (C0123…) to (id, info_dict). Tries an ID lookup first (authoritative), then falls back to a name search over the channels the bot can see.\n\n\n\n\n\n","title":"SlackClaw.resolve_channel"},{"category":"method","location":"api/#SlackClaw.route_socket_event!-Tuple{MonitorState, Dict}","page":"API Reference","text":"Route one Socket Mode message event into the regular dispatch paths.\n\n\n\n\n\n","title":"SlackClaw.route_socket_event!"},{"category":"method","location":"api/#SlackClaw.run_agent_loop!-Tuple{MonitorState, String, String, String}","page":"API Reference","text":"run_agent_loop!(state, thread_ts, prompt, session_id; react_ts=\"\", channel_id=state.config.slack_channel_id)\n\nRun Claude in a loop, handling [CONTINUE] and [SCHEDULE] directives. Posts each response to the thread. Watches status file during execution.\n\n\n\n\n\n","title":"SlackClaw.run_agent_loop!"},{"category":"method","location":"api/#SlackClaw.run_claude-Tuple{String, SlackClawConfig}","page":"API Reference","text":"run_claude(prompt::String, config::SlackClawConfig) -> ClaudeResult\n\nRun claude CLI in --print mode with JSON output. Blocks until complete or timeout.\n\n\n\n\n\n","title":"SlackClaw.run_claude"},{"category":"method","location":"api/#SlackClaw.run_monitor-Tuple{SlackClawConfig}","page":"API Reference","text":"run_monitor(config::SlackClawConfig)\n\nBlocking entry point for a single channel. Connects via Slack Socket Mode and serves pushed message events until interrupted (Ctrl-C). Requires config.app_token (an xapp- app-level token with connections:write) and errors at startup if it is empty. To serve many channels of one workspace over a single socket, use run_socket_fleet. Fatal errors are appended to .slackclaw_crash.log in repo_dir before rethrowing.\n\n\n\n\n\n","title":"SlackClaw.run_monitor"},{"category":"method","location":"api/#SlackClaw.run_socket_fleet-Tuple{Vector{SlackClawConfig}}","page":"API Reference","text":"run_socket_fleet(configs::Vector{SlackClawConfig})\n\nBlocking entry point serving MANY channels over ONE Socket Mode connection — the correct shape for full-workspace push delivery. Each config gets its own MonitorState (threads, persistence, scheduled tasks, proactive checks, budgets); every inbound event is offered to every state, and states ignore channels they don't serve. All configs must share the workspace's bot and app tokens and resolve to distinct state files (override state_file for channels sharing a repo_dir).\n\n\n\n\n\n","title":"SlackClaw.run_socket_fleet"},{"category":"method","location":"api/#SlackClaw.save_state!-Tuple{MonitorState}","page":"API Reference","text":"Save full monitor state (threads, last_ts, scheduled tasks) to disk.\n\n\n\n\n\n","title":"SlackClaw.save_state!"},{"category":"method","location":"api/#SlackClaw.setup-Tuple{}","page":"API Reference","text":"setup(; repo_dir=pwd(), app_name=\"SlackClaw\", write_env=true)\n\nInteractive setup wizard. Writes the manifest to slackclaw_manifest.yaml, prints it with the grouped browser steps, waits while you do them, then prompts for the two tokens and the channel and runs verify_setup. For scripted / non-interactive use, call generate_manifest and verify_setup directly.\n\n\n\n\n\n","title":"SlackClaw.setup"},{"category":"method","location":"api/#SlackClaw.should_process-Tuple{SlackMessage, SlackClawConfig, Dict}","page":"API Reference","text":"should_process(msg::SlackMessage, config::SlackClawConfig, raw::Dict) -> Bool\n\nReturn true if this message should be dispatched to Claude. Skips bot messages, messages from our own bot user, and empty text.\n\n\n\n\n\n","title":"SlackClaw.should_process"},{"category":"method","location":"api/#SlackClaw.slack_add_reaction-Tuple{SlackClawConfig, AbstractString, AbstractString}","page":"API Reference","text":"slack_add_reaction(config, ts, emoji; channel_id=config.slack_channel_id)\n\nAdd an emoji reaction to a message. Silently ignores \"already_reacted\" errors.\n\n\n\n\n\n","title":"SlackClaw.slack_add_reaction"},{"category":"method","location":"api/#SlackClaw.slack_apps_connections_open-Tuple{SlackClawConfig}","page":"API Reference","text":"slack_apps_connections_open(config) -> String\n\nRequest a Socket Mode websocket URL via apps.connections.open. Authenticates with the app-level token (xapp-, scope connections:write), not the bot token. The returned wss:// URL is single-use and short-lived.\n\n\n\n\n\n","title":"SlackClaw.slack_apps_connections_open"},{"category":"method","location":"api/#SlackClaw.slack_auth_test-Tuple{SlackClawConfig}","page":"API Reference","text":"slack_auth_test(config) -> String\n\nCall auth.test and return the bot's own user ID.\n\n\n\n\n\n","title":"SlackClaw.slack_auth_test"},{"category":"method","location":"api/#SlackClaw.slack_auth_test_info-Tuple{SlackClawConfig}","page":"API Reference","text":"slack_auth_test_info(config) -> Dict\n\nCall auth.test and return the full response dict (user_id, user, team, team_id, url). Validates the bot token; used by setup for a friendly report.\n\n\n\n\n\n","title":"SlackClaw.slack_auth_test_info"},{"category":"method","location":"api/#SlackClaw.slack_conversations_info-Tuple{SlackClawConfig, AbstractString}","page":"API Reference","text":"slack_conversations_info(config, channel_id) -> Dict\n\nFetch channel metadata. Returns the full channel info dict.\n\n\n\n\n\n","title":"SlackClaw.slack_conversations_info"},{"category":"method","location":"api/#SlackClaw.slack_conversations_join-Tuple{SlackClawConfig, AbstractString}","page":"API Reference","text":"slack_conversations_join(config, channel_id) -> Dict\n\nJoin a public channel (conversations.join, scope channels:join). Private channels cannot be self-joined — a human must /invite the bot.\n\n\n\n\n\n","title":"SlackClaw.slack_conversations_join"},{"category":"method","location":"api/#SlackClaw.slack_conversations_list-Tuple{SlackClawConfig}","page":"API Reference","text":"slack_conversations_list(config; types=\"public_channel,private_channel\") -> Vector\n\nList channels the bot can see, following pagination. Each entry has at least id and name. Used by setup to resolve a channel name to its ID.\n\n\n\n\n\n","title":"SlackClaw.slack_conversations_list"},{"category":"method","location":"api/#SlackClaw.slack_get_history-Tuple{SlackClawConfig, AbstractString}","page":"API Reference","text":"slack_get_history(config, oldest; channel_id=config.slack_channel_id) -> Vector{Dict}\n\nFetch channel messages newer than oldest timestamp. Returns raw message dicts in chronological order (oldest first).\n\n\n\n\n\n","title":"SlackClaw.slack_get_history"},{"category":"method","location":"api/#SlackClaw.slack_get_replies-Tuple{SlackClawConfig, AbstractString, AbstractString}","page":"API Reference","text":"slack_get_replies(config, thread_ts, oldest; channel_id=config.slack_channel_id) -> Vector{Dict}\n\nFetch replies in a thread newer than oldest. Returns in chronological order. The first message (the thread parent) is always included by the API; we skip it.\n\n\n\n\n\n","title":"SlackClaw.slack_get_replies"},{"category":"method","location":"api/#SlackClaw.slack_post_message-Tuple{SlackClawConfig, AbstractString}","page":"API Reference","text":"slack_post_message(config, text; thread_ts=\"\", channel_id=config.slack_channel_id) -> String\n\nPost a message to a channel. Returns the message ts. If thread_ts is non-empty, posts as a thread reply.\n\n\n\n\n\n","title":"SlackClaw.slack_post_message"},{"category":"method","location":"api/#SlackClaw.slack_request-Tuple{Symbol, String, SlackClawConfig}","page":"API Reference","text":"slack_request(method, endpoint, config; query=nothing, body=nothing) -> Dict\n\nMake authenticated Slack API request. Handles JSON parsing and rate-limit (429) retries.\n\n\n\n\n\n","title":"SlackClaw.slack_request"},{"category":"method","location":"api/#SlackClaw.slack_ts_now-Tuple{}","page":"API Reference","text":"Format a Unix timestamp as a Slack-compatible ts string (no scientific notation).\n\n\n\n\n\n","title":"SlackClaw.slack_ts_now"},{"category":"method","location":"api/#SlackClaw.slack_upload_file-Tuple{SlackClawConfig, AbstractString}","page":"API Reference","text":"slack_upload_file(config, file_path; thread_ts=\"\", channel_id=config.slack_channel_id,\n                  title=\"\", initial_comment=\"\") -> Dict\n\nUpload a file to Slack using the V2 three-step flow:\n\nfiles.getUploadURLExternal — get pre-signed upload URL\nPOST file bytes to the upload URL\nfiles.completeUploadExternal — finalize and share to channel/thread\n\n\n\n\n\n","title":"SlackClaw.slack_upload_file"},{"category":"method","location":"api/#SlackClaw.socket_event_consumer!-Tuple{Vector{MonitorState}, Channel}","page":"API Reference","text":"Dispatch consumer: drains the event queue in FIFO order, offering each event to every state in the fleet (a state's classifier ignores channels it doesn't serve). Exactly one consumer must run — cursor claims assume per-channel ts ordering, and an out-of-order claim would advance the cursor past an undispatched message, losing it permanently (reconciliation never refetches behind a cursor). Claude work still fans out via the @async pool inside the dispatch functions; only the pre-dispatch API calls are serialized here.\n\n\n\n\n\n","title":"SlackClaw.socket_event_consumer!"},{"category":"method","location":"api/#SlackClaw.socket_fleet_loop!-Tuple{Vector{MonitorState}}","page":"API Reference","text":"Socket Mode main loop for a fleet of monitor states sharing ONE websocket: connect, read frames, reconnect with backoff. Slack load-balances an app's events across its open sockets, so all channels of a workspace must be served by exactly one connection — this loop is that connection. Runs until any state's running flips false.\n\n\n\n\n\n","title":"SlackClaw.socket_fleet_loop!"},{"category":"method","location":"api/#SlackClaw.socket_housekeeping!-Tuple{MonitorState}","page":"API Reference","text":"Fire scheduled tasks/proactive checks and the periodic reconciliation poll.\n\n\n\n\n\n","title":"SlackClaw.socket_housekeeping!"},{"category":"method","location":"api/#SlackClaw.socket_loop!-Tuple{MonitorState}","page":"API Reference","text":"Single-channel Socket Mode is a fleet of one.\n\n\n\n\n\n","title":"SlackClaw.socket_loop!"},{"category":"method","location":"api/#SlackClaw.socket_selftest-Tuple{SlackClawConfig}","page":"API Reference","text":"socket_selftest(config; timeout_s=15) -> Bool\n\nOpen a Socket Mode connection and wait for Slack's hello frame, then close. Confirms the app token + connections:write end to end. Returns true if the hello arrives before the timeout. apps.connections.open failures (bad app token) propagate to the caller.\n\n\n\n\n\n","title":"SlackClaw.socket_selftest"},{"category":"method","location":"api/#SlackClaw.start_monitor-Tuple{SlackClawConfig}","page":"API Reference","text":"start_monitor(config::SlackClawConfig) -> MonitorState\n\nAuthenticate with Slack, load persisted state, resolve listen-channel names, and post the startup banner. Returns the MonitorState without entering a message loop — use run_monitor for the blocking entry point, or run socket_loop! yourself and call stop_monitor! when done.\n\n\n\n\n\n","title":"SlackClaw.start_monitor"},{"category":"method","location":"api/#SlackClaw.stop_monitor!-Tuple{MonitorState}","page":"API Reference","text":"stop_monitor!(state::MonitorState)\n\nStop the monitor: flips state.running (loops and background tasks exit at their next check), waits for in-flight dispatch tasks, and posts a shutdown notice to the primary channel.\n\n\n\n\n\n","title":"SlackClaw.stop_monitor!"},{"category":"method","location":"api/#SlackClaw.thread_last_active-Tuple{SlackClaw.ThreadSession}","page":"API Reference","text":"Last-activity time for a thread session: newest reply seen, or creation time.\n\n\n\n\n\n","title":"SlackClaw.thread_last_active"},{"category":"method","location":"api/#SlackClaw.validate_fleet-Tuple{Vector{SlackClawConfig}}","page":"API Reference","text":"Validate that a fleet of configs can share one socket: same workspace (bot/app token), a non-empty app token, unique primary channels, and no two channels resolving to the same state file (last-writer-wins corruption).\n\n\n\n\n\n","title":"SlackClaw.validate_fleet"},{"category":"method","location":"api/#SlackClaw.verify_setup-Tuple{}","page":"API Reference","text":"verify_setup(; bot_token, app_token, channel, repo_dir=pwd(),\n             write_env=true, env_path=joinpath(repo_dir, \"slackclaw.env\"),\n             announce=true) -> NamedTuple\n\nThe automated half of setup. Given the two tokens and a channel name/ID, this: validates the bot token (auth.test), resolves the channel, auto-joins it when public, runs a live Socket Mode self-test with the app token, writes a gitignored slackclaw.env, and posts a confirmation to the channel. Prints a pass/fail checklist and returns (; ok, channel_id, bot_user_id, team, checks). Re-runnable and side-effect-light (the only channel post is the confirmation).\n\n\n\n\n\n","title":"SlackClaw.verify_setup"},{"category":"method","location":"api/#SlackClaw.watch_status_file-Tuple{SlackClawConfig, String, Ref{Bool}}","page":"API Reference","text":"Watch status file during Claude execution, posting updates to thread.\n\n\n\n\n\n","title":"SlackClaw.watch_status_file"},{"category":"section","location":"features/#Features","page":"Features","text":"All features are toggles or fields on SlackClawConfig. Defaults are in the configuration table.","title":"Features"},{"category":"section","location":"features/#Agent-directives","page":"Features","text":"When agent_directives is enabled (default), Claude can control its own execution flow by ending a response with a directive:\n\n[CONTINUE] — immediately re-invoke in the same session for multi-step work.\n[CONTINUE: next step description] — re-invoke with a specific follow-up prompt.\n[SCHEDULE: 2h: check pipeline results] — queue a future invocation; durations accept 30m, 1h, 2h30m, and the like.\n\nIf no directive is present the task is complete. Consecutive [CONTINUE]s are capped at max_continue (default 10) as a runaway guard; scheduled tasks survive restarts via .slackclaw_state.json.","title":"Agent directives"},{"category":"section","location":"features/#Multi-channel-listening","page":"Features","text":"A monitor can listen to channels beyond its primary one. Set listen_channel_ids:\n\nrun_monitor(SlackClawConfig(\n    slack_channel_id   = \"C_PRIMARY\",\n    listen_channel_ids = [\"C_GENERAL\", \"C_ANNOUNCE\"],\n    repo_dir           = \"/path/to/repo\",\n))\n\nMessages from listen channels run through a relevance filter first: Claude is asked whether the message is relevant to this instance's repo/role and answers [SKIP] if not. Only relevant messages are posted — as a new thread in the primary channel, prefixed with the source channel name — and handled there. The bot must be a member of each listen channel; channel names are resolved on startup and cached. Listen-channel events arrive live on the same socket as the primary channel; the only extra cost is that the periodic reconciliation also backfills each listen channel's history.","title":"Multi-channel listening"},{"category":"section","location":"features/#Proactive-mode","page":"Features","text":"When proactive_enabled = true, SlackClaw periodically runs Claude to check for things worth reporting (every proactive_interval_s, default 1 h) — but only once it has an agenda: it fires only when .slackclaw_proactive_tasks exists in repo_dir (or proactive_prompt is set). Claude reads two files in repo_dir at runtime:\n\n.slackclaw_proactive_tasks — what to check. Editable anytime without a restart, so you can adjust the agenda live.\n.slackclaw_proactive_log — what was already reported (auto-appended). Claude reads it to avoid repeating itself.\n\nIf nothing is noteworthy, Claude responds [SKIP] and stays silent; only real content is posted as a top-level message in the primary channel.\n\nrun_monitor(SlackClawConfig(\n    proactive_enabled    = true,\n    proactive_interval_s = 1800,   # every 30 min\n))\n\nFrequency can be changed live by posting in the monitored channel:\n\nproactive every 30m (or proactive interval 2h) — adjust the interval\nproactive on / proactive off — toggle\n\nThe proactive_prompt field can carry inline suggestions, but prefer .slackclaw_proactive_tasks so the agenda hot-reloads.","title":"Proactive mode"},{"category":"section","location":"features/#Progress-updates-during-long-tasks","page":"Features","text":"While Claude runs, SlackClaw watches status_file (.slackclaw_status in repo_dir, polled every status_poll_s). Any non-empty content present when it polls is posted to the thread as a progress update (a value overwritten between polls can be missed), and the file is cleared when the task finishes — letting a long task narrate itself rather than going silent.","title":"Progress updates during long tasks"},{"category":"section","location":"features/#Sending-files-and-images","page":"Features","text":"When Claude runs under SlackClaw with an active thread, it can attach a file or image to its reply. SlackClaw injects SLACKCLAW_UPLOAD (the path to the bundled bin/slack-upload helper) plus the thread context (SLACKCLAW_THREAD_TS, SLACKCLAW_CHANNEL_ID, SLACKCLAW_BOT_TOKEN) into the subprocess environment, and appends a short instruction to the system prompt telling Claude the helper exists. So \"plot X and send it back\" needs no extra wiring: Claude saves the figure and runs $SLACKCLAW_UPLOAD <file_path> [\"caption\"], which posts it into the current thread via Slack's external upload flow (files.getUploadURLExternal + files.completeUploadExternal). It needs curl and jq on the host and the files:write scope — included in the setup manifest, but apps created before this release must add files:write and reinstall.","title":"Sending files and images"},{"category":"section","location":"features/#Concurrency-and-thread-limits","page":"Features","text":"Each message is dispatched as an async task; at most max_concurrent_tasks (default 5) run at once. Messages that arrive while at the cap get a brief \"busy\" reply.\nA busy thread that receives another message reports elapsed time instead of starting a second run.\nAt most max_active_threads (default 3) conversation threads are tracked; the oldest are retired first. Threads idle longer than max_thread_idle_s (default 7 days; 0 disables) are also retired — each tracked thread costs a conversations.replies call per reconciliation, so this bounds steady load.","title":"Concurrency and thread limits"},{"category":"section","location":"features/#Persistence","page":"Features","text":"State is saved to state_file (.slackclaw_state.json in repo_dir): the primary cursor, tracked thread sessions, scheduled tasks, per-listen-channel cursors, and the last proactive timestamp. It is loaded on startup, so sessions and schedules survive restarts. Channels that share a repo_dir must each set a distinct state_file. ```","title":"Persistence"},{"category":"section","location":"socket-mode/#Socket-Mode-(Push-Delivery)","page":"Socket Mode","text":"SlackClaw receives messages over Slack Socket Mode: it opens an outbound websocket to Slack (apps.connections.open) and Slack pushes message events as they happen. This is the only live delivery mechanism — there is no polling mode to enable or disable (a low-frequency reconciliation still backfills history; see Reliability below).\n\nWhy Socket Mode:\n\nSub-second latency — events are pushed, not polled.\nNo continuous history polling — only a low-frequency reconciliation fetch (every reconcile_interval_s), so rate-limit pressure is minimal.\nListen channels ride the same socket: every channel the bot is in arrives on one connection (the reconciliation poll still backfills each one's history).\nNo public endpoint is needed (unlike the HTTP Events API): the connection is outbound-only, so it works behind firewalls and NAT.\n\nPrerequisites (the setup() wizard provisions all of these): an app-level xapp- token with connections:write in SLACK_APP_TOKEN, Socket Mode enabled on the app, and the message.* event subscriptions. A missing or empty app_token is a startup error.","title":"Socket Mode (Push Delivery)"},{"category":"section","location":"socket-mode/#Usage","page":"Socket Mode","text":"using SlackClaw\n\nrun_monitor(SlackClawConfig(\n    repo_dir = \"/path/to/repo\",\n    app_token = ENV[\"SLACK_APP_TOKEN\"],   # default; shown for clarity\n    reconcile_interval_s = 300,           # gap-fill reconciliation cadence (default 5 min)\n))\n\nEverything downstream of message receipt — dispatching, threading, directives, listen-channel relevance filtering, proactive checks, persistence — is the same regardless of how a message arrived (a live socket event or the reconciliation poll). Replies go out over the bot token (chat.postMessage); the socket is events-in only.","title":"Usage"},{"category":"section","location":"socket-mode/#How-it-works","page":"Socket Mode","text":"","title":"How it works"},{"category":"section","location":"socket-mode/#Event-flow","page":"Socket Mode","text":"apps.connections.open (authenticated with the app-level token) returns a short-lived wss:// URL; SlackClaw connects with HTTP.WebSockets.\nSlack pushes each event wrapped in an envelope. The websocket read loop does three things only — parse, ack immediately, enqueue — and never touches the Slack Web API. Slack redelivers envelopes not acked within ~3 s, so any blocking work in the read loop (even a single rate-limit retry sleep) would starve the acks of frames queued behind it.\nA single FIFO consumer task drains the queue: each event is classified (top-level message in the primary channel, reply in a tracked thread, listen-channel message, or ignorable) and routed into the dispatch functions. The consumer stays single deliberately: cursor claims rely on per-channel timestamp ordering, and an out-of-order claim would advance a cursor past an undispatched message — a loss reconciliation could never detect. Claude invocations themselves still fan out to the concurrent task pool inside dispatch.","title":"Event flow"},{"category":"section","location":"socket-mode/#Reliability:-reconciliation-and-cursors","page":"Socket Mode","text":"Push delivery is at-least-once, and events that fire while the connection is down are not replayed by Slack. SlackClaw keeps message cursors as a safety net:\n\nA reconciliation poll — a full history fetch of anything newer than the cursors (primary channel, tracked threads, listen channels) — fires on every (re)connect and every reconcile_interval_s (default 300 s).\nEvery message, from either path, must claim its cursor (primary-channel last_ts, per-thread last_reply_ts, or per-listen-channel cursor) under a lock before dispatch. First claim wins; replays lose.\n\nThe result: socket events, reconciliation fetches, and Slack redeliveries can overlap freely and a message is still dispatched exactly once.","title":"Reliability: reconciliation and cursors"},{"category":"section","location":"socket-mode/#Connection-lifecycle","page":"Socket Mode","text":"Slack refreshes Socket Mode connections every few hours by design:\n\nA disconnect frame with reason warning arrives ~1 min ahead — SlackClaw keeps serving.\nOn refresh_requested (or any close/error), SlackClaw requests a fresh URL and reconnects — immediate after a healthy connection, exponential backoff (capped at 60 s) while connections are failing.\nEach successful (re)connect triggers a reconciliation poll to fill whatever gap the downtime left.\n\nScheduled tasks ([SCHEDULE] directives) and proactive checks are time-driven, not event-driven; a background housekeeping task fires them on the poll_interval_s cadence.","title":"Connection lifecycle"},{"category":"section","location":"socket-mode/#One-socket-per-workspace","page":"Socket Mode","text":"Slack load-balances events across an app's open Socket Mode connections — each event is delivered to exactly one of them. Two SlackClaw socket monitors under the same app would each receive a random half of the traffic. Serve a whole workspace from a single connection — use run_socket_fleet below for multiple channels, and give separate workspaces separate apps.","title":"One socket per workspace"},{"category":"section","location":"socket-mode/#Serving-a-whole-workspace:-run_socket_fleet","page":"Socket Mode","text":"To run all of a workspace's channels on push, use the fleet entry point — many channels, one socket, one process:\n\nbot = ENV[\"SLACK_BOT_TOKEN\"]; app = ENV[\"SLACK_APP_TOKEN\"]\n\nconfigs = [SlackClawConfig(slack_bot_token=bot, app_token=app,\n                           slack_channel_id=id, repo_dir=dir)\n           for (id, dir) in channels]\nrun_socket_fleet(configs)\n\nEach config keeps its own monitor state — threads, persistence, scheduled tasks, proactive checks, and budgets behave exactly as in a single-channel monitor — and every pushed event is offered to every state, which ignores channels it doesn't serve. Startup validation requires: identical bot and app tokens across the fleet (one fleet = one workspace), unique primary channels, and unique state-file paths — fleet startup errors if two channels resolve to the same state_file, so channels sharing a repo_dir must each set a distinct one (a shared status_file is allowed but warns, since status updates could cross-post). (run_monitor for a single channel is just a fleet of one.)","title":"Serving a whole workspace: run_socket_fleet"},{"category":"section","location":"socket-mode/#Configuration-reference","page":"Socket Mode","text":"Field Default Description\napp_token get(ENV, \"SLACK_APP_TOKEN\", \"\") Required. App-level xapp- token, scope connections:write\nreconcile_interval_s 300 Seconds between gap-fill reconciliation polls\npoll_interval_s 10 Cadence of the housekeeping task (scheduled/proactive checks, reconcile tick)","title":"Configuration reference"},{"category":"section","location":"socket-mode/#Troubleshooting","page":"Socket Mode","text":"Symptom Likely cause\nnot_allowed_token_type on connect app_token is a bot (xoxb-) token — use the app-level xapp- token\ninvalid_auth on connect App-level token revoked, or belongs to a different app\napp_token is empty at startup SLACK_APP_TOKEN not set in the monitor's environment\nConnects (Socket Mode connected logged) but never reacts to messages Event subscriptions missing, or app not reinstalled after adding them\nSome messages handled, others silently missed A second Socket Mode connection is open for the same app — events are being split\nBot deaf in one channel only Bot not a member — /invite @SlackClaw there\nMessages handled twice Should not happen (cursor claims); check that two monitors aren't watching the same channel with different repo_dir state files\n\n```","title":"Troubleshooting"},{"category":"section","location":"use-cases/#Use-Cases","page":"Use Cases","text":"Each use case below is set up once on a channel's monitor. Do the one-time Slack App Setup first and source slackclaw.env; the examples then assume SLACK_BOT_TOKEN / SLACK_APP_TOKEN / SLACK_CHANNEL_ID are set. Every config field shown is per-channel — in a run_socket_fleet deployment, set the same fields on each member config.","title":"Use Cases"},{"category":"section","location":"use-cases/#Fix-a-bug-or-add-a-feature","page":"Use Cases","text":"Post a request in the channel; Claude works in the repo and replies in the thread.\n\nSetup — point a monitor at the repo:\n\nrun_monitor(SlackClawConfig(repo_dir = \"/path/to/repo\", model = \"sonnet\"))\n\nClaude has full tool access in repo_dir (it can edit files and run the tests), but it does not commit on its own. To make commits part of the workflow, append a policy to the system prompt (the field is mutable, so you keep the default Slack-formatting prompt):\n\ncfg = SlackClawConfig(repo_dir = \"/path/to/repo\")\ncfg.system_prompt *= \"\\n\\nCommit your changes with a clear message before replying.\"\nrun_monitor(cfg)\n\nUse it — in the channel: \"Fix the off-by-one in parse_header and add a test.\" Reply in the thread to iterate; the same Claude session is resumed.","title":"Fix a bug or add a feature"},{"category":"section","location":"use-cases/#Get-a-plot-back-as-an-image","page":"Use Cases","text":"Claude generates a figure and posts it to the thread.\n\nSetup:\n\nProvision with SlackClaw.setup() — its manifest already includes the files:write scope. (Apps created before v0.4.0 must add files:write and reinstall.)\nInstall curl and jq on the host (the upload helper uses them).\n\nNothing else is needed: bin/slack-upload ships with the package, and SlackClaw appends an upload instruction to the system prompt automatically whenever a message is dispatched in a thread.\n\nUse it — \"Plot the loss curve from logs/run42.csv and send it back.\" Claude saves an image and runs $SLACKCLAW_UPLOAD <file> under the hood.","title":"Get a plot back as an image"},{"category":"section","location":"use-cases/#Periodically-scan-your-repos","page":"Use Cases","text":"On a schedule, Claude runs the checks you define and posts only when there's something worth flagging.\n\nSetup — enable proactive mode and set the interval:\n\nrun_monitor(SlackClawConfig(\n    repo_dir = \"/path/to/repo\",\n    proactive_enabled = true,\n    proactive_interval_s = 14400,     # every 4 h\n))\n\nThen write the checks to .slackclaw_proactive_tasks in repo_dir (plain text, editable anytime — it's re-read each cycle):\n\n- List open PRs that need review\n- Report any failing CI runs\n- Flag branches with no commits in 30+ days\n\nProactive mode fires only when this file exists (or proactive_prompt is set). SlackClaw appends a summary of each post to .slackclaw_proactive_log, which Claude reads for context to avoid repeats. Adjust live from the channel: proactive every 2h, proactive off, proactive on.","title":"Periodically scan your repos"},{"category":"section","location":"use-cases/#Kick-off-a-long-job-and-walk-away","page":"Use Cases","text":"Claude starts a long task, optionally posts progress, and schedules a follow-up.\n\nSetup — agent_directives is on by default, so Claude can end a reply with [SCHEDULE: 2h: check the run and report results] to be re-invoked later. For live progress, tell Claude to write to the status file — SlackClaw polls it every status_poll_s and posts changes to the thread:\n\ncfg = SlackClawConfig(repo_dir = \"/path/to/repo\")\ncfg.system_prompt *= \"\\n\\nFor long tasks, write one-line progress updates to \" *\n                     \".slackclaw_status in your working directory.\"\nrun_monitor(cfg)\n\nUse it — \"Start the training run; post progress and tell me when it's done.\"","title":"Kick off a long job and walk away"},{"category":"section","location":"use-cases/#Triage-across-repos-from-one-channel","page":"Use Cases","text":"Post in a shared hub channel; each repo's monitor answers only if the message is relevant to it.\n\nSetup — invite the bot to the hub channel, then serve the workspace's repo channels from one socket with run_socket_fleet, giving each member the hub in its listen_channel_ids:\n\nbot = ENV[\"SLACK_BOT_TOKEN\"]; app = ENV[\"SLACK_APP_TOKEN\"]\n\nrun_socket_fleet([\n    SlackClawConfig(slack_bot_token=bot, app_token=app, slack_channel_id=\"C_REPO_A\",\n                    repo_dir=\"/repo/a\", listen_channel_ids=[\"C_HUB\"]),\n    SlackClawConfig(slack_bot_token=bot, app_token=app, slack_channel_id=\"C_REPO_B\",\n                    repo_dir=\"/repo/b\", listen_channel_ids=[\"C_HUB\"]),\n])\n\nUse the fleet, not one run_monitor per repo: a workspace's events are load-balanced across an app's open sockets, so separate per-channel monitors under one app would each see only a subset (separate workspaces get separate apps). Each hub message runs through a relevance filter — only the repos it's relevant to reply, each posting a new thread in its own channel.\n\nUse it — in the hub: \"Anyone's tests failing after the JSON bump?\" Only the affected repos respond, each in its own channel.","title":"Triage across repos from one channel"},{"category":"section","location":"use-cases/#Ask-about-a-codebase","page":"Use Cases","text":"Ask a question; Claude reads the repo and explains — from your phone, no terminal.\n\nSetup — just a monitor on the repo channel; nothing extra.\n\nUse it — \"How does the reconciliation loop avoid double-dispatch?\"","title":"Ask about a codebase"},{"category":"section","location":"#SlackClaw","page":"Home","text":"Slack-to-Claude Code bridge: SlackClaw watches Slack channels, dispatches messages to the claude CLI as subprocesses, and posts the results back as threaded replies. Messages arrive in real time over Slack Socket Mode (push).\n\nnote: vs. the Slack MCP plugin\nThe Slack MCP plugin gives an interactive Claude session tools to operate Slack (read, search, post). SlackClaw is the inverse — Slack as a frontend to autonomous Claude Code agents on your repos, triggered by incoming messages over Socket Mode, with no human in a Claude client. They are complementary: a SlackClaw-dispatched agent can itself load the MCP plugin for richer Slack tooling.","title":"SlackClaw"},{"category":"section","location":"#Example-use-cases","page":"Home","text":"Fix a bug or add a feature from Slack — Claude edits files and runs the tests in repo_dir, then replies in the thread (ask it to commit — SlackClaw doesn't on its own); reply in-thread to iterate (same session resumed).\nGet a plot back as an image — Claude generates a figure and uploads it to the thread (file/image upload is built in).\nPeriodically scan your repos — configure proactive mode to ask Claude on a schedule to check for new PRs, failing CI, or stale branches, posting only when there's something worth saying.\nKick off a long job and walk away — Claude can post progress by writing the status file and schedules a follow-up with [SCHEDULE: 2h: …].\nTriage across repos from one channel — each repo's monitor picks up only what's relevant to it (relevance-filtered listening) and answers in its own channel.\nAsk about a codebase from your phone — Claude reads the repo and explains, no terminal needed.\n\nSee Use Cases for step-by-step setup of each.","title":"Example use cases"},{"category":"section","location":"#Features","page":"Home","text":"Real-time push — messages arrive over Slack Socket Mode (sub-second), with a reconciliation poll as a gap-fill safety net\nGuided setup — SlackClaw.setup() provisions the Slack app from a manifest and wires up the tokens\nMulti-turn sessions — thread replies resume the same Claude session (--resume)\nAgent directives — Claude controls its own flow with [CONTINUE] and [SCHEDULE: 2h: ...]\nConcurrent tasks — each message runs as an async task, capped by max_concurrent_tasks\nMulti-channel listening — read-only channels with a relevance filter; responses land in the primary channel\nProactive mode — periodic autonomous checks that post only when there is something to say\nPersistence — cursors, thread sessions, and scheduled tasks survive restarts (.slackclaw_state.json)","title":"Features"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"SlackClaw\")                                       # once registered in General\n# before registration: Pkg.add(url=\"https://github.com/kalidke/SlackClaw.jl\")\n\nRequires Julia ≥ 1.10 and an authenticated claude CLI on the host.","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"First provision the Slack app — the wizard handles everything that can be automated (see Slack App Setup):\n\nusing SlackClaw\nSlackClaw.setup()\n\nsetup() writes slackclaw.env. source slackclaw.env, then run a monitor:\n\nrun_monitor(SlackClawConfig(repo_dir = \"/path/to/your/repo\"))   # blocking; Ctrl-C to stop\n\nTo serve many channels of one workspace on a single socket:\n\nrun_socket_fleet([\n    SlackClawConfig(slack_channel_id=\"C_A\", repo_dir=\"/repo/a\"),\n    SlackClawConfig(slack_channel_id=\"C_B\", repo_dir=\"/repo/b\"),\n])","title":"Quick start"},{"category":"section","location":"#How-a-message-flows","page":"Home","text":"A user posts in the monitored channel; Slack pushes the event over the socket.\nSlackClaw reacts with 👀 and spawns claude --print in repo_dir, with the message as the prompt.\nThe response is posted as a threaded reply (long responses are split across multiple messages).\nReplies in that thread resume the same Claude session; ✅ / ❌ reactions mark completion.\nIf the response ends in [CONTINUE], Claude is re-invoked immediately; [SCHEDULE: 30m: check results] queues a future invocation.","title":"How a message flows"},{"category":"section","location":"#Documentation","page":"Home","text":"Slack App Setup — the setup() wizard, the app manifest, tokens, channel wiring\nUse Cases — step-by-step setup for each use case\nFeatures — agent directives, multi-channel listening, proactive mode, progress updates\nSocket Mode — push delivery: how it works, reliability, the fleet, troubleshooting\nAPI Reference — all docstrings\n\n```","title":"Documentation"},{"category":"section","location":"setup/#Slack-App-Setup","page":"Slack App Setup","text":"SlackClaw provisions its own Slack app. The wizard automates everything that an API can do and groups the human-only steps into a single browser session, so setup is generate → (do the browser steps once) → verify, with no back-and-forth.\n\nusing SlackClaw\nSlackClaw.setup()","title":"Slack App Setup"},{"category":"section","location":"setup/#What-you-do-vs.-what-the-wizard-does","page":"Slack App Setup","text":"Only three things genuinely require a human, and they are contiguous in the Slack web UI:\n\nCreate the app from the manifest. At api.slack.com/apps → Create New App → From an app manifest → pick the workspace → paste the manifest the wizard printed → Create. The manifest pre-sets every bot scope, the message-event subscriptions, and Socket Mode — there is nothing to toggle by hand.\nInstall to workspace. Install App → Install to Workspace → Allow, then copy the Bot User OAuth Token (xoxb-…).\nMint the app-level token. Basic Information → App-Level Tokens → Generate Token and Scopes → add connections:write → Generate, then copy the token (xapp-…).\n\nOne extra step only for a private channel: /invite @SlackClaw in it. Public channels are joined automatically.\n\nPaste the two tokens and the channel name/ID back into the wizard. From there it is automated — the wizard:\n\nvalidates the bot token (auth.test) and reports the workspace,\nresolves the channel name to its ID,\nauto-joins the channel if it is public and the bot isn't a member,\nruns a live Socket Mode self-test with the app token (opens the websocket and waits for Slack's hello),\nwrites a gitignored slackclaw.env (chmod 600) with the three values,\nposts a confirmation to the channel,\nprints a pass/fail checklist and the command to start the monitor.","title":"What you do vs. what the wizard does"},{"category":"section","location":"setup/#The-manifest","page":"Slack App Setup","text":"println(SlackClaw.generate_manifest())\n\nThe bot scopes and events it requests, and why:\n\nBot scope Purpose\nchannels:history / groups:history Read public / private channel messages\nchannels:read / groups:read Resolve channel names and info\nchannels:join Auto-join public channels during setup\nim:history Direct messages (message.im)\nchat:write Post messages and thread replies\nreactions:write Status reactions (👀 ✅ ❌)\nfiles:write Upload files to threads\n\nMessage events (delivered over the socket): message.channels, message.groups, message.im.\n\nnote: Two token types\nThe bot token (xoxb-) does all Slack Web API work — reading history, posting replies, reactions, uploads. The app-level token (xapp-, scope connections:write) only authorizes the Socket Mode websocket; it cannot call Web API methods, and a bot token cannot open a socket (not_allowed_token_type).","title":"The manifest"},{"category":"section","location":"setup/#Non-interactive-setup","page":"Slack App Setup","text":"For scripted or CI use, drive the two halves directly instead of the wizard:\n\n# 1. Print the manifest to create the app from.\nprintln(SlackClaw.generate_manifest(; app_name = \"SlackClaw\"))\n\n# 2. After creating/installing the app and minting the app token, verify & wire:\nSlackClaw.verify_setup(;\n    bot_token = \"xoxb-…\",\n    app_token = \"xapp-…\",\n    channel   = \"#dev\",        # name or ID\n    write_env = true,          # write slackclaw.env (default)\n    announce  = true,          # post a confirmation to the channel (default)\n)\n\nverify_setup returns (; ok, channel_id, bot_user_id, team, checks) and is safe to re-run.","title":"Non-interactive setup"},{"category":"section","location":"setup/#Multiple-channels-and-workspaces","page":"Slack App Setup","text":"Many channels, one workspace: run a single run_socket_fleet process. Slack delivers each event to exactly one of an app's open sockets, so the whole workspace must share one connection — do not run a separate socket monitor per channel under the same app.\nSeparate workspaces get separate apps and token sets (e.g. SLACK_BOT_TOKEN_KAL / SLACK_BOT_TOKEN_ATQI resolved per process). Run setup() once per workspace.","title":"Multiple channels and workspaces"},{"category":"section","location":"setup/#Verify","page":"Slack App Setup","text":"The wizard's self-test already confirms the chain end to end. To check a running monitor, start it: it posts _SlackClaw monitor started_ to the channel with a configuration summary. Post a message — within ~1 s the bot reacts with 👀 and replies in a thread. ```","title":"Verify"}]
}
